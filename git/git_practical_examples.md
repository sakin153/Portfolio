# Git - Практические примеры и шаблоны

## Установка и первичная настройка

### Установка Git на разных ОС

**Windows:**
```bash
# Скачать с официального сайта
# https://git-scm.com/download/win
# Устанавливается Git Bash - эмулятор командной строки
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt install git
```

**macOS:**
```bash
# Через Homebrew
brew install git

# Или скачать с официального сайта
# https://git-scm.com/download/mac
```

### Первичная настройка Git
```bash
# Настройка имени пользователя
git config --global user.name "Ваше Имя"

# Настройка email
git config --global user.email "your.email@example.com"

# Настройка редактора по умолчанию
git config --global core.editor "code --wait"  # для VS Code
git config --global core.editor "nano"         # для nano

# Проверка настроек
git config --list
```

## Базовые команды с примерами

### Инициализация и клонирование
```bash
# Инициализация нового репозитория
git init

# Клонирование существующего репозитория
git clone https://github.com/user/repo.git

# Клонирование с определённым именем папки
git clone https://github.com/user/repo.git my-project
```

### Ежедневные операции
```bash
# Проверка статуса
git status

# Добавление файлов
git add filename.txt
git add .  # добавить все файлы
git add *.js  # добавить все JS файлы

# Коммит изменений
git commit -m "Описание изменений"
git commit -am "Добавить и закоммитить все изменённые файлы"

# Отправка изменений
git push origin main
git push origin feature-branch

# Получение изменений
git pull origin main
git fetch  # только загрузить, не мержить
```

### Создание и настройка репозитория

#### Создание локального репозитория
```bash
# Создать папку для проекта
mkdir my-project
cd my-project

# Инициализировать Git репозиторий
git init

# Проверить статус
git status

# Создать первый файл
echo "# My Project" > README.md

# Добавить файл в индекс
git add README.md

# Сделать первый коммит
git commit -m "Initial commit: Add README"
```

#### Связывание с удалённым репозиторием
```bash
# Добавить удалённый репозиторий (после создания на GitHub)
git remote add origin https://github.com/username/repository.git

# Проверить настроенные удалённые репозитории
git remote -v

# Отправить изменения на удалённый сервер
git push -u origin main

# При последующих push можно использовать просто:
git push
```

#### Клонирование существующего репозитория
```bash
# Клонировать репозиторий
git clone https://github.com/username/repository.git

# Клонировать с определённым именем папки
git clone https://github.com/username/repository.git my-local-name
```

### Работа с индексом (Staging Area)
```bash
# Добавить файлы в индекс
git add filename.txt           # конкретный файл
git add .                     # все файлы в текущей папке
git add *.js                  # все JS файлы
git add src/                  # всю папку src

# Убрать файл из индекса (но оставить изменения)
git reset filename.txt
git rm --cached filename.txt  # если файл был добавлен впервые

# Посмотреть что в индексе
git status
git diff --cached             # разница между индексом и последним коммитом
```

### Работа с ветками
```bash
# Создание новой ветки
git branch feature-new-function
git checkout -b feature-new-function  # создать и переключиться

# Переключение между ветками
git checkout main
git checkout feature-new-function

# Список веток
git branch  # локальные ветки
git branch -r  # удалённые ветки
git branch -a  # все ветки

# Слияние веток
git checkout main
git merge feature-new-function

# Удаление ветки
git branch -d feature-new-function  # безопасное удаление
git branch -D feature-new-function  # принудительное удаление
```

## Шаблоны для презентаций

### Слайд "Что такое Git?"
**Заголовок:** Git - современная система контроля версий

**Содержание:**
- Отслеживание изменений в файлах
- Совместная работа над проектами
- История всех модификаций
- Возможность отката к любой версии

### Слайд "Преимущества Git"
- 🚀 **Скорость** - локальная работа
- 🌿 **Ветвление** - лёгкое создание веток
- 🔄 **Распределённость** - каждая копия - полный репозиторий
- 🛡️ **Надёжность** - контрольные суммы для целостности данных

### Слайд "Базовый рабочий процесс"
1. **Изменение файлов** в рабочей директории
2. **Добавление в индекс** (`git add`)
3. **Фиксация изменений** (`git commit`)
4. **Отправка на сервер** (`git push`)

## Типичные сценарии использования

### Сценарий 1: Новая функция
```bash
# 1. Переключиться на main
git checkout main
git pull origin main

# 2. Создать ветку для функции
git checkout -b feature-user-auth

# 3. Работать над кодом...
# 4. Добавить и закоммитить изменения
git add .
git commit -m "Добавить аутентификацию пользователей"

# 5. Отправить ветку на сервер
git push origin feature-user-auth

# 6. Создать Pull Request / Merge Request
```

### Сценарий 2: Исправление ошибки
```bash
# 1. Создать hotfix ветку из main
git checkout main
git checkout -b hotfix-login-bug

# 2. Исправить ошибку
# 3. Закоммитить исправление
git add .
git commit -m "Исправить ошибку входа в систему"

# 4. Влить в main
git checkout main
git merge hotfix-login-bug

# 5. Отправить изменения
git push origin main

# 6. Удалить hotfix ветку
git branch -d hotfix-login-bug
```

## Полезные алиасы для .gitconfig
```bash
# Сокращения команд
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.cm commit
git config --global alias.lg "log --oneline --decorate --all --graph"
```

## Специальные файлы Git

### .gitignore - игнорирование файлов
```bash
# Создать .gitignore файл
touch .gitignore

# Примеры содержимого .gitignore:

# Зависимости
node_modules/
vendor/
*.log

# IDE файлы
.vscode/
.idea/
*.swp
*.swo

# OS файлы
.DS_Store
Thumbs.db

# Конфигурационные файлы
.env
.env.local
config.local.js

# Файлы сборки
dist/
build/
*.min.js

# Временные файлы
tmp/
temp/
*.tmp
```

### .gitkeep - сохранение пустых папок
```bash
# Git не отслеживает пустые папки
# Чтобы добавить пустую папку в репозиторий:

mkdir logs
touch logs/.gitkeep
git add logs/.gitkeep

# .gitkeep - это просто соглашение, можно использовать любое имя
# Главное - чтобы в папке был хотя бы один файл
```

### Шаблоны .gitignore для разных проектов

**Node.js проект:**
```bash
node_modules/
npm-debug.log*
.npm
.env
dist/
```

**Python проект:**
```bash
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
.venv/
.env
```

**Java проект:**
```bash
*.class
*.jar
target/
.gradle/
build/
```

## Структура коммит-сообщений
```
<тип>: <краткое описание>

<подробное описание>

<подвал>
```

**Типы коммитов:**
- `feat` - новая функция
- `fix` - исправление ошибки
- `docs` - документация
- `style` - форматирование кода
- `refactor` - рефакторинг
- `test` - тесты
- `chore` - рутинные задачи

## Команды для экстренных ситуаций
```bash
# Отменить последний коммит (не отправленный)
git reset --soft HEAD~1

# Отменить изменения в файле
git checkout -- filename.txt

# Временно сохранить изменения
git stash
git stash pop  # восстановить

# Посмотреть историю
git log --oneline
git log --graph --all --oneline --decorate
```
